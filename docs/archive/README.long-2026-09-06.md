# k-curriculum-2022

> **2022 개정 교육과정 성취기준 + 교과 간 융합 연결 그래프**
> 오픈 데이터셋 · 추출 알고리즘 · 융합수업 설계를 위한 지식 그래프

[![Data License: CC BY 4.0](https://img.shields.io/badge/data-CC%20BY%204.0-blue.svg)](./LICENSE-DATA)
[![Code License: MIT](https://img.shields.io/badge/code-MIT-green.svg)](./LICENSE)
[![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)](./CONTRIBUTING.md)

한국 2022 개정 교육과정의 **성취기준 6,444개**와, 서로 다른 교과의 성취기준을
**융합 관점으로 잇는 연결 3,874개**를 담은 공개 데이터셋입니다. 연결을 만들어낸
**추출 알고리즘**(임베딩 후보 추출 → LLM 판정)도 함께 공개합니다.

교사·연구자·에듀테크 개발자가 **융합(교과 통합) 교육**을 하는 데 조금이라도 도움이
되었으면 하는 마음에 작업한 결과물을, 저의 **첫 오픈소스 프로젝트**로 공개합니다.

---

## 📄 원문 대조는 이렇게 하세요

이 데이터셋의 성취기준은 아래 **교육부 원문**을 파싱·정제한 것입니다. 값에 의문이
들면 언제든 원문과 대조해 주세요.

- **원문 출처**: 교육부 고시 제2022-33호 「초·중등학교 교육과정」 (2022.12.22)
  및 교과별 각론 고시
- **원문 열람**: 국가교육과정정보센터(NCIC) <https://ncic.re.kr>
  <!-- 총론·교과별 각론의 심층 링크는 관리자가 보강합니다 -->

원문 파일(PDF·HWP)은 이 저장소에 **재호스팅하지 않습니다.** 교육부/NCIC를 언제나
최신·정본으로 두고, 이 저장소는 원문 링크와 그 파싱 결과만 관리합니다.

> 참고로, 이 원문을 *기계가 읽도록* 만드는 일이 왜 그토록 어려웠는지는 아래
> [부록: 원문 데이터의 파싱 난점](#부록--교육부-원문-데이터의-파싱-난점)과, 그로부터
> 이어지는 [교육부에 드리는 제언](#차기-교육과정-데이터에-드리는-제언-초안)에서
> 실제 사례로 설명합니다.

---

## ⚠️ 먼저 읽어주세요 — 이 데이터는 완벽하지 않습니다

이 데이터셋은 공공 고시 원문을 파싱하고, 교과 간 연결은 **AI로 생성**했습니다.
그 과정에서 **오류가 남아 있을 수 있습니다** — 성취기준 본문의 미세한 손상,
억지스러운 연결, 학년군 표기 불일치 등. 알려진 문제는 [KNOWN_ISSUES.md](./KNOWN_ISSUES.md)에
투명하게 정리해 두었습니다.

**오류를 발견하면 묻어두지 말고 알려주세요.** 이 프로젝트는 커뮤니티의 교정으로
좋아지는 것을 전제로 설계했습니다.

- 🐛 **오류 신고**: [이슈 열기](../../issues/new/choose) (성취기준 오류 / 억지 연결 신고 템플릿 제공)
- 🔧 **직접 고치기**: [PR 보내기](./CONTRIBUTING.md) — 단 한 글자 수정도 환영합니다

> "지금 이 결과물은 작은 눈 뭉치라고 생각합니다. 많은 분들의 참여와 AI의 발전을 통해
> 이 눈 뭉치가 멋진 눈사람이 되어, 교육적 가치를 만들어내는 데이터셋이 되길 소망합니다."

---

## 왜 · 어떤 아이디어로 · 어디로

**왜 만들었나.** 융합 수업을 설계하려면 "지금 이 교과의 이 성취기준과, 다른 교과의
무엇을 이으면 좋을까?"라는 질문에 답해야 합니다. 그런데 교육과정 문서는 교과별로
따로 존재해서, 교과의 경계를 **넘나드는 지도**가 없었습니다. 그 지도를 만들어보고
싶었습니다 — 성취기준을 점(node)으로, 교과를 잇는 융합의 실마리를 선(edge)으로 삼은
**하나의 그래프**로.

**어떤 아이디어로.** 두 가지 원칙을 지켰습니다.
- **"찾기"와 "판단"의 분리** — 후보는 임베딩 유사도로 결정적으로 좁히고(재현 가능),
  교육적 타당성만 AI가 판단합니다. 그래서 결과가 매번 흔들리지 않습니다.
- **"AI가 만들게 두지 않는다"** — AI에게 성취기준을 *번호로만* 참조하게 해서, 없는
  코드를 지어내는 할루시네이션을 구조적으로 막았습니다.
  (자세히 → [추출 알고리즘](#추출-알고리즘--사람을-위한-설명))

**어디로 (개선점).** 지금은 시작입니다. 생각하고 있는 방향은:
- 연결의 **커버리지 불균형** 해소 — 데이터가 얇은 학년군·교과(특히 1~2학년군)로 확장
- **candidate 11,817건**의 커뮤니티 검토를 통한 published 승격
- 사람이 신고한 **억지 연결**을 학습해 판정 기준을 정교화
- 임베딩·판정 모델을 **더 나은 것으로** 교체하며 재생성 (그래프는 모델과 함께 자랍니다)
- 표준 포맷(JSON-LD·CSV·그래프DB) export로 **재사용성** 확대
  (전체 목록 → [로드맵](#로드맵--아직-안-된-것들))

---

## 데이터 규모

| 파일 | 내용 | 규모 |
|------|------|------|
| [`data/standards.json`](./data/standards.json) | 성취기준 (2022 개정) | **6,444개** / 292과목 · 고유 식별자는 `key` |
| [`data/links.published.json`](./data/links.published.json) | 검토·정제된 융합 연결 | **3,874개** (quality ≥ 0.70, 평균 0.83 · 전문교과끼리의 연결은 제외) |
| [`data/links.candidate.json`](./data/links.candidate.json) | AI 제안 연결 (미검토) | **11,817개** (품질 편차 큼) |

필드 정의·생성 방법·한계는 **[데이터 카드](./data/DATACARD.md)** 참고.

## 빠른 시작

```bash
git clone https://github.com/greatsong/k-curriculum-2022.git
cd k-curriculum-2022
```

```js
// 성취기준 로드
import standards from './data/standards.json' assert { type: 'json' }
import links from './data/links.published.json' assert { type: 'json' }

// 예: 특정 성취기준과 융합 가능한 상대 교과 성취기준 찾기
const code = '[6실05-05]'
const byCode = Object.fromEntries(standards.map(s => [s.code, s]))
const partners = links
  .filter(l => l.source_code === code || l.target_code === code)
  .map(l => {
    const other = l.source_code === code ? l.target_code : l.source_code
    return { 성취기준: byCode[other]?.content, 융합주제: l.integration_theme, 수업아이디어: l.lesson_hook }
  })
console.log(partners)
```

더 많은 예시는 [`examples/`](./examples) 참고.

## 데이터 구조

**성취기준** (`standards.json`의 각 항목):
```jsonc
{
  "code": "[4과01-01]",           // 성취기준 코드 (2022 개정 표기, 대괄호 포함)
  "subject": "과학",
  "subject_group": "과학",
  "grade_group": "초3-4",          // 학년군 (초1-2 / 초3-4 / 초5-6 / 중1-3 / 고공통 / 고선택 / 기타)
  "school_level": "초등학교",
  "curriculum_category": "공통",
  "content": "일상생활에서 힘과 관련된 현상에…",  // 성취기준 본문
  "keywords": ["일상생활", "관찰", …],
  "explanation": "…",             // 성취기준 해설 (있는 경우)
  "application_notes": "…"         // 적용 시 고려사항 (있는 경우)
}
```

**융합 연결** (`links.*.json`의 각 항목):
```jsonc
{
  "source_code": "[4과01-01]",
  "target_code": "[4수02-03]",
  "link_type": "cross_subject",    // cross_subject / same_concept / application / prerequisite / extension
  "rationale": "두 성취기준이 …로 연결되는 근거",
  "integration_theme": "힘과 측정",  // 융합 주제
  "lesson_hook": "물체를 밀고 당기며 …",  // 수업 아이디어 한 줄
  "semantic_score": 0.71,          // 임베딩 코사인 유사도 (0~1, 실측)
  "quality_score": 0.85,           // LLM 판정 교육적 품질 (0~1)
  "generation_method": "ai"
}
```

## 추출 알고리즘 — 사람을 위한 설명

교과 간 연결은 어떻게 만들었을까요? 직관부터 이야기하겠습니다.

**문제.** "국어의 이 성취기준과 과학의 저 성취기준이 융합 수업으로 이어질까?"를
사람이 일일이 판단하면, 6,444개 성취기준의 조합은 **2,000만 쌍**이 넘습니다.
사람은 다 볼 수 없고, 그렇다고 AI에게 "교과 두 개 줄 테니 연결을 나열해"라고 하면
두 가지가 무너집니다 — ① 매번 답이 달라지고(재현 불가), ② **존재하지 않는 성취기준
코드를 지어냅니다**(할루시네이션).

**해법 — 찾는 일과 판단하는 일을 분리한다.** 마치 도서관에서 사서가 먼저 "주제가
비슷한 책"을 좁혀 오면(1단계), 전문가가 그중 진짜 쓸 만한 것을 고르는(2단계) 것과
같습니다.

- **1단계 — 후보 좁히기 (계산, 결정적).** 모든 성취기준을 의미 벡터(임베딩)로 바꾼
  뒤, 벡터가 서로 가까운(의미가 비슷한) 쌍만 추립니다. 순수 계산이라 **몇 번을
  돌려도 같은 결과**가 나오고, 여기서 나온 실제 유사도가 `semantic_score`입니다.
- **2단계 — 교육적으로 판단하기 (LLM).** 좁혀진 후보만 AI에게 보여주되, **성취기준을
  "1번, 2번" 같은 번호로만** 가리키게 합니다. AI는 코드를 *쓸 수 없고 고르기만* 하므로,
  없는 코드를 지어내는 일이 원천적으로 불가능합니다. AI는 이 연결이 교육적으로
  타당한지 판단해 품질 점수·연결 유형·융합 주제·수업 아이디어를 만듭니다.
- **게시 기준.** 그중 품질 점수 0.70 이상만 `published`로 올립니다.

핵심은 **"AI가 자유롭게 만들게 두지 않는다"** 입니다. 찾기는 계산에 맡겨 재현성을
확보하고, AI에게는 *좁혀진 선택지 안에서 고르고 설명하는* 역할만 줍니다. 이 구조가
할루시네이션을 막고 결과를 검증 가능하게 합니다.

실행 코드는 [`algorithm/`](./algorithm), 자세한 실행법은
[algorithm/README.md](./algorithm/README.md) 참고.

---

## AI 에이전트를 위한 작업 명세서

이 데이터셋을 **재현·확장**하려는 AI 에이전트(또는 자동화 파이프라인)를 위한 계약
수준의 명세입니다. 아래를 지키면 결과가 이 저장소의 `data/`와 동일한 규격으로
나옵니다.

### 0. 불변식 (INVARIANTS — 반드시 지킬 것)
- **INV-1 코드 무결성**: 출력 링크의 `source_code`·`target_code`는 반드시
  `data/standards.json`에 존재하는 `code`여야 한다. **새 코드 생성 금지.**
- **INV-2 정규화**: 모든 링크는 `source_code < target_code`(유니코드 사전순)로 정렬.
- **INV-3 점수 범위**: `semantic_score`, `quality_score` ∈ [0, 1].
- **INV-4 결정성**: 1단계(후보 추출)는 동일 입력에 동일 출력을 낸다(무작위성 금지).
- **INV-5 검증 통과**: 산출물은 `node scripts/validate.mjs`를 오류 0으로 통과해야 한다.

### 1. 입력 (INPUT)
- `data/standards.json` — 성취기준 배열. 각 항목의 계약: `code`(string, 식별자),
  `content`(string, 임베딩 대상 본문), `subject`/`subject_group`/`grade_group` 등 메타.
- **임베딩 캐시** — `{ "embeddings": { "<code>": number[] } }` 형태 JSON.
  각 벡터는 해당 `code`의 `content`를 임베딩한 것. 차원은 일관되어야 함(모델 자유).
  용량 문제로 리포에 미포함 → 에이전트가 생성. (원 데이터는 OpenAI 임베딩 사용)

### 2. 처리 단계 (PROCEDURE)

**Stage 1 — 후보쌍 추출 (결정적)**
1. 각 성취기준 벡터를 L2 정규화한다(코사인 = 내적).
2. 각 성취기준에 대해 다른 성취기준과의 코사인 유사도를 계산하고 **상위 top-k**
   (기본 `k=6`)를 남긴다.
3. 유사도 `< min-cos`(기본 0.45) 후보는 버린다.
4. 쌍을 INV-2로 정규화하고, **이미 존재하는 쌍**(`data/links.*.json`)은 제외한다.
5. 산출: `{ source_code, target_code, semantic_score }` 후보 목록.

**Stage 2 — LLM 판정 (코드 할루시네이션 차단 프로토콜)**
1. 후보를 배치(기본 25쌍)로 묶는다.
2. 프롬프트에 성취기준을 **번호(index)로만** 제시한다. 예: `1. <content A>` /
   `2. <content B>`. 모델에게 코드 문자열을 절대 노출·요구하지 않는다.
3. 모델은 각 후보에 대해 다음 스키마의 JSON을 **인덱스로 참조**해 반환한다:
   ```jsonc
   { "pair": [1, 2], "valid": true, "link_type": "cross_subject",
     "quality_score": 0.85, "rationale": "...", "integration_theme": "...",
     "lesson_hook": "..." }
   ```
4. **응답 검증·폐기 규칙**: `link_type`이 허용 집합
   (`cross_subject|same_concept|application|prerequisite|extension`)이 아니거나,
   `quality_score`가 범위를 벗어나거나, `pair` 인덱스가 배치 범위를 벗어나면 **그 판정은
   폐기**한다(재시도 가능). 모델이 만든 코드 문자열은 **무시**하고, 인덱스로 원본
   코드를 역참조해 채운다(INV-1 보장).
5. 산출: 원본 코드로 복원한 링크 레코드.

**Stage 3 — 게시 정책 (선택)**
- `quality_score ≥ 0.70` → `published`, 그 미만 → `candidate`.
- 임계값은 정책 파라미터이며 `algorithm/promote-links.mjs`로 사후 조정 가능.

### 3. 출력 (OUTPUT 계약)
`data/DATACARD.md`의 링크 필드 규격을 따른다. 필수:
`source_code, target_code, link_type, rationale, integration_theme, lesson_hook,
semantic_score, quality_score, generation_method`.

### 4. 파라미터 (기본값)
| 이름 | 기본 | 의미 |
|------|------|------|
| `top-k` | 6 | 성취기준당 후보 수 |
| `min-cos` | 0.45 | 후보 최소 코사인 |
| `batch-size` | 25 | 배치당 후보 쌍 수 |
| `concurrency` | 3 | 동시 판정 배치 수 |
| 게시 임계 | 0.70 | published 승격 하한 |

### 5. 확장 지침 (AGENT DIRECTIVES)
- **모델 교체 시**: 2단계 판정 모델을 바꿔도 위 프로토콜(인덱스 참조·검증·폐기)은
  그대로 유지한다. 코드 노출 금지 규칙을 완화하지 말 것.
- **임베딩 교체 시**: 벡터 차원만 일관되면 무방. 교체 후 `semantic_score` 분포가
  달라지므로 `min-cos`를 재보정한다.
- **대량 생성 후**: 반드시 `scripts/validate.mjs`로 참조 무결성(dangling)·정규화·점수
  범위를 확인하고, 실패 시 게시하지 않는다(INV-5).
- **품질 보고**: 커버리지·기각률·게시 비율을 함께 남겨, 무엇이 누락됐는지 은폐하지
  않는다.

## 재현하고 더 잘 만들고 싶은 분들께 — 팁

명세서가 "무엇을"이라면, 이건 작업하며 배운 "실전 감각"입니다.

- **품질의 8할은 임베딩에서 갈립니다.** 성취기준 `content`는 짧아서, 본문만 임베딩하면
  표면적 유사도에 치우칩니다. `content`에 `explanation`·`keywords`를 덧붙여 임베딩하면
  recall이 눈에 띄게 좋아집니다. 한국어 특화 임베딩 모델도 시도해볼 만합니다.
- **`min-cos`는 하나로 고정하지 마세요.** 교과군을 *넘는* 융합(교차군)은 유사도가
  낮게 나오고, *같은* 교과군 내 계열성은 높게 나옵니다. 단일 임계값보다 **교과쌍
  성격에 따른 적응형 임계값**이 더 좋은 후보를 남깁니다.
- **"작위적 융합"을 경계하세요.** 둘 다 '환경'을 언급한다고 이어버리면 억지 연결이
  됩니다. 판정 프롬프트에서 *"이 둘을 한 수업으로 어떻게 잇는가"* 를 반드시 쓰게 하면,
  설명이 안 되는 표면 유사 연결이 걸러집니다. `lesson_hook`이 억지스러우면 그 연결도
  의심하세요.
- **방향이 있는 연결은 방향을 지키세요.** `prerequisite`(선수)·`extension`(확장)은
  순서가 의미입니다. 저장은 `source < target`로 정규화하되, **방향은 `link_type`으로**
  보존해야 "무엇이 무엇의 선수학습인지"가 남습니다.
- **검증을 사람 눈이 아니라 CI에 맡기세요.** `scripts/validate.mjs`를 PR 게이트로 걸면,
  참조가 깨진 링크(dangling)나 정규화 위반이 병합되는 사고를 원천 차단합니다.
  (실제로 이 데이터도 성취기준 버전이 어긋나 링크 500여 건이 깨졌던 걸 이 검증이
  잡아냈습니다.)
- **더 잘 만드는 지름길은 "사람의 판단을 되먹이는 것"입니다.** 커뮤니티가 신고한 억지
  연결과 승격/강등 이력은 그 자체로 훌륭한 학습 신호입니다. 이 신호로 판정 기준을
  조정하면, 모델을 키우지 않고도 그래프가 좋아집니다.

## 로드맵 — 아직 안 된 것들

이 데이터셋은 진행 중입니다. 솔직하게 남은 과제를 적어둡니다.

- [ ] **학년군 표기 완전 정규화** — 일부 학년군이 오염 표기(`초2`→`초1-2` 등)에서
      정규화됐으나, 근본 원인인 원본 파싱 파이프라인 정비는 미완 ([KNOWN_ISSUES](./KNOWN_ISSUES.md))
- [ ] **성취기준 본문 잔존 손상 교정** — 해설/적용사항 혼입(explanation bleed) 등 일부 잔존
- [ ] **1~2학년군 확충** — 현재 국어·수학만 존재. 통합교과(바슬즐)·안전한생활 미포함
- [ ] **전문교과("기타" 1,490개) 메타데이터 보강** — school_level·학년군 미표기 다수
- [ ] **candidate 링크 검토 승격** — 11,817개 미검토 연결의 커뮤니티 리뷰
- [ ] **다국어 필드** — 영문 요약/키워드
- [ ] **표준 포맷 export** — RDF/JSON-LD, Neo4j import, CSV

각 항목은 이슈로도 열려 있습니다. **함께 해주실 분을 찾습니다.**

## 부록 — 교육부 원문 데이터의 파싱 난점

이 데이터셋을 만드는 작업의 상당 부분은 "연결을 찾는 일"이 아니라, **원문 문서를
기계가 읽을 수 있게 만드는 일**이었습니다. 2022 개정 교육과정 고시 문서(주로 HWP)는
사람이 눈으로 보기엔 정연하지만, 프로그램으로 파싱하려는 순간 **일관성 없는 규칙**이
곳곳에서 발목을 잡았습니다. 대표적인 세 가지를 실제 데이터로 보입니다.

### 사례 1 — 성취기준 "코드"가 6가지 구조로 제각각 (식별자에 공백·로마숫자)

성취기준 코드는 데이터의 **기본 식별자(primary key)** 입니다. 그런데 그 표기 형식이
교과에 따라 최소 6가지로 갈립니다.

| 실제 코드 | 구조 | 문제 |
|-----------|------|------|
| `[2국01-01]` | 숫자+과목+영역-순번 | 표준형 |
| `[공관 01-01-01]` | 과목약칭+**공백**+3단 숫자 | **식별자 안에 공백** (전문교과 **1,490개**) |
| `[12미적Ⅰ-01-01]` | …+**로마숫자 Ⅰ/Ⅱ**+… | 아라비아 숫자와 **로마숫자 혼용** |

식별자에 **공백**이 들어가면(예: `[공관 01-01-01]`, 게다가 과목명 자체가 "공정 관리",
"금융 일반"처럼 공백 포함) 토큰 분리가 깨지고, **로마숫자 `Ⅰ`·`Ⅱ`**(유니코드
U+2160대)가 섞이면 단일 정규식으로는 코드를 뽑아낼 수 없습니다. "코드 하나만 안정적으로
추출"하는 데도 교과군마다 예외 처리를 쌓아야 했습니다.

### 사례 2 — 학년이 코드 첫 숫자에 "비선형 암호"로 숨어 있음

학년(학년군)이 별도 필드로 명시되지 않고, **코드 첫 숫자에 암묵적으로** 인코딩돼
있습니다. 그런데 그 매핑이 직관과 어긋나고 중간에 규칙이 깨집니다.

```
[2국01-01]  → 첫 숫자 2  = 초등 1~2학년군   (2학년이 아님!)
[4과01-01]  → 첫 숫자 4  = 초등 3~4학년군
[6실05-05]  → 첫 숫자 6  = 초등 5~6학년군      … 여기까지 "N → (N-1)~N"
[9…]        → 첫 숫자 9  = 중학교 1~3학년군    … 갑자기 규칙이 바뀜
[10…] [12…] → 고등학교                          … 10과 12가 혼재
```

숫자 `2`를 순진하게 "2학년"으로 읽으면 **틀립니다**(실제로는 1~2학년군). 이 "말도
안 되는 규칙성" 때문에, 우리 데이터에도 한동안 학년군이 `초2`(사실은 초1~2)처럼
**오염된 채** 저장돼 있었고, 별도의 정규화 단계를 만들어 894건을 교정해야 했습니다
([KNOWN_ISSUES](./KNOWN_ISSUES.md) 참고). 학년이라는 핵심 메타데이터가 **문서 어디에도
명시적 필드로 없고**, 오직 코드 관례를 역공학해야만 복원됩니다.

### 사례 3 — 본문·해설·표·수식이 한 덩어리로 뭉개짐

HWP 표 셀에서 텍스트를 추출하면, **성취기준 본문(content)** 과 **해설·적용 시 고려사항**,
심지어 **표 머리글·페이지 번호**가 한 문단으로 병합돼 나오는 경우가 많았습니다
(explanation bleed). 문장은 임의 위치에서 줄바꿈(`\n`)으로 끊기고(현재 데이터에도
본문의 약 5%에 개행이 남아 있습니다), 어디까지가 성취기준이고 어디부터가 해설인지
**구조적 경계가 없습니다**.

특히 수학·과학의 **수식**은 HWP 전용 수식 폰트의 **사설 영역(PUA, U+E000–U+F8FF)
글리프**로 저장돼 있어, 다른 환경에서 열면 `√`·`×`·`ⁿ` 같은 기호가 **□(두부)로
깨집니다**. 텍스트로는 복원 불가능해, 결국 교사가 원문을 눈으로 보며 13건을 손으로
다시 입력해야 했습니다(추정 금지, 원문 대조).

> **요컨대**, 가장 기초적인 세 가지 — *식별자를 안정적으로 뽑고 · 학년을 알아내고 ·
> 본문의 경계를 나누는 것* — 조차 문서가 기계를 배려하지 않아 매번 예외 처리와 수작업
> 복원이 필요했습니다. 이 저장소가 원본까지 함께 공개하는 이유이기도 합니다.

## 차기 교육과정 데이터에 드리는 제언 (초안)

<!-- 초안입니다. 관리자(송석리)가 최종 문구를 다듬습니다. -->

먼저, 방대한 교육과정을 정리해 고시로 공개해 주신 분들의 노고에 감사드립니다. 아래는
그 문서를 **데이터로 옮기는 작업을 직접 해 본 한 교사**로서, 다음 교육과정 문서는
조금 더 쉽게 재사용될 수 있기를 바라는 마음에서 드리는 개인적인 제언입니다.

핵심은 하나입니다. **사람이 읽을 문서(HWP·PDF)와 별도로, 기계가 읽을 데이터를 함께
공개해 주십시오.** 지금은 좋은 내용이 사람 눈에만 맞춰진 형식에 갇혀 있어, 이를 활용
하려는 모두가 같은 파싱 고생을 처음부터 반복합니다.

구체적으로, 위 [파싱 난점](#부록--교육부-원문-데이터의-파싱-난점)에서 겪은 문제에
대응해 다음을 제안드립니다.

1. **기계가독 포맷 동시 공개** — HWP와 함께 CSV 또는 JSON(가능하면 관계를 표현하는
   JSON-LD)으로도 배포. 표 안의 데이터가 표 밖에서도 살아 있게.
2. **식별자는 식별자답게** — 성취기준 코드에서 공백과 로마숫자(Ⅰ·Ⅱ)를 없애고, 전
   교과에 걸쳐 **하나의 일관된 규칙**으로. 코드는 사람 라벨이 아니라 기계 키입니다.
3. **메타데이터를 본문에 숨기지 말 것** — 학년(학년군)·학교급·교과·영역을 코드 첫
   숫자에 암호처럼 인코딩하지 말고, **각각 별도 필드(열)로** 명시. 특히 "1~2학년군"을
   `2`로 적는 방식은 오해를 부릅니다.
4. **본문과 해설의 구조적 분리** — 성취기준·해설·적용 시 고려사항을 한 셀에 병합하지
   말고 별도 필드로. 표 머리글·페이지 번호가 본문에 섞이지 않도록.
5. **수식·특수문자는 표준으로** — 특정 프로그램 전용 폰트의 사설 영역(PUA) 글자 대신
   유니코드 표준을 사용하고, 수식은 가능하면 MathML/LaTeX를 병기. 다른 환경에서 열어도
   □로 깨지지 않게.
6. **버전·라이선스를 기계가독으로** — 개정 버전과 변경 이력, 이용 조건(공공누리 유형)을
   데이터에 함께 명시. 그래야 안심하고 오래 재사용할 수 있습니다.

이 여섯 가지는 대단한 기술이 아니라 **약속**에 가깝습니다. 문서를 만들 때 이 약속만
지켜 주셔도, 저 같은 사람들이 파싱과 복원에 쏟는 수백 시간이 교육 콘텐츠를 만드는
시간으로 바뀝니다. 공교육이 만든 교육과정 데이터가 **공공재답게** 널리, 오래, 정확하게
재사용되기를 바랍니다.

<sub>— 이 제언은 개인의 바람이며, 교육부·NCIC의 공식 입장과 무관합니다.</sub>

## 라이선스

- **데이터** (`data/`): [CC BY 4.0](./LICENSE-DATA) — 출처 표시 후 자유 이용.
  성취기준 원문의 저작권 원천은 **교육부(2022 개정 교육과정 고시)** 이며, 재이용 시
  교육부 출처를 함께 표기해 주세요.
- **코드** (`algorithm/`, `examples/`): [MIT](./LICENSE)

## 인용

연구·출판에 사용하실 때는 [CITATION.cff](./CITATION.cff)를 참고해 인용해 주세요.

## 기여

모든 규모의 기여를 환영합니다 — 오타 하나부터 알고리즘 개선까지.
[CONTRIBUTING.md](./CONTRIBUTING.md)에서 시작하세요.

---

<sub>이 데이터셋은 융합수업 협력 설계 플랫폼 **curriculum-weaver** 프로젝트에서
추출·정리되었습니다. 한국 융합교육 생태계의 공동 자산이 되기를 바랍니다.</sub>
